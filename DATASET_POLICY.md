# 데이터셋 정책 — CASE A only

최종 확정: 2026-09-17

---

## 핵심 원칙

> **확실하게 확인된 실제 몬스터 + 정확한 Ground Truth bbox = CASE A**
> **CASE A가 아니면 학습 데이터에 절대 포함하지 않는다.**

---

## CASE 분류 기준

| CASE | 학습 사용 | 보존 |
|------|-----------|------|
| A | ✅ 사용 | ✅ |
| B | ❌ 제외 | ✅ (분석용) |
| C | ❌ 제외 | ✅ (분석용) |
| E | ❌ 제외 | ✅ (분석용) |
| HARD | ❌ 제외 | ✅ (분석용) |
| EMPTY | ❌ 제외 | ✅ (분析용) |

---

## CASE A 판정 기준 (모두 충족해야 함)

- 실제 살아있는 몬스터가 확실하게 보임
- 몬스터 위치를 명확하게 확인 가능
- bbox를 정확하게 그릴 수 있음
- 다른 오브젝트와 혼동 가능성 낮음
- 시체 아님 / 아데나·아이템 아님 / 플레이어 아님
- 배경 오브젝트·그림자·풀·바위 아님
- 앞뒤 프레임 확인 후 실제 몬스터임을 확신
- **애매하면 A로 승격 금지 → B/C/HARD로 분류**

---

## 새 영상 처리 순서 (반드시 준수)

```
새 영상 입력
    ↓
몬스터 확인 (YOLO 보조 + Tracker 보조)
    ↓
CASE A GT 생성 (영상 화면 기준, YOLO 결과 복사 금지)
    ↓
stride 적용 (연속 중복 제거)
    ↓
A급 데이터셋 생성 (dataset_field_A_vN/)
    ↓
Preview / Contact Sheet 생성
    ↓
통계 보고 (아래 항목)
    ↓
사용자 직접 검수
    ↓
사용자가 "학습 시작" 명시적 승인
    ↓
그때만 학습 실행
```

---

## 보고 필수 항목 (학습 전 반드시 제출)

1. A급 총 개수
2. 새로 추가된 A급 개수
3. 중복 제거 후 개수
4. train / val / test 개수
5. monster bbox 개수 / adena bbox 개수
6. 위치 분포 (LEFT / CENTER / RIGHT)
7. 크기 분포
8. 기존 A급 데이터와 중복 여부
9. Preview 이미지
10. Contact Sheet
11. 데이터 품질 검사 결과

---

## Tracking 사용 규칙

- Tracking은 **A급 GT 확정 보조 수단**으로만 사용
- 확인 요소: bbox 위치/크기/IoU/이동 방향/속도/앞뒤 프레임/외형/주변 객체
- cx/cy 단독 판단 금지
- Tracking 결과를 그대로 GT로 복사 금지

---

## 기존 YOLO 사용 규칙

- 참고 기준 모델: `runs/detect/monster_v2/weights/best.pt`
- YOLO detection 결과를 GT로 복사 금지
- 최종 A급 판정은 실제 영상 화면 기준
- 기존 모델 절대 덮어쓰기/삭제 금지

---

## 데이터셋 디렉터리 규칙

- 기존 `dataset_field_v1/` 보존 (삭제/수정 금지)
- 새 영상마다 독립 디렉터리 생성: `dataset_field_A_vN/`
- 이미지는 images/로, 라벨은 labels/로, 미리보기는 previews/로

---

## 모델 저장 규칙

- 새 모델은 별도 디렉터리에 저장: `runs/detect/monster_A_vN/`
- 기존 `runs/detect/monster_v2/` 절대 덮어쓰기/삭제 금지

---

## 학습 후 평가 항목

- mAP@50, mAP@50-95, Precision, Recall
- 위치별 탐지율 (좌/중/우/상/하)
- 크기별 탐지율 (소/중/대)
- 오탐(FP) 증가 여부
- 기존 monster_v2 대비 비교

---

## 데이터 축적 방식

- 새 영상 → 기존 A급에 없는 새로운 상황의 몬스터만 추가
- 단순 프레임 수 늘리기 금지
- 유지 우선: 위치/크기/자세/배경/화면위치 변화가 있는 프레임
- 버전 관리: dataset_field_A_v1 → v2 → v3 ...

